undo
----

vars: (cmove)
from
to
captured (type of piece captured)
moved (type of piece moved)
mov2 (rook move in castling)

vars: (engine)
moveshistory

mothods: cmovestack
pop()

changes: cmovestack
push() should save which type of piece was captured

main:
add "undo" command